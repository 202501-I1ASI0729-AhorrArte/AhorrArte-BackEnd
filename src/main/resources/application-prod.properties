# Production Configuration
spring.application.name=AhorrArte-Platform

# Spring DataSource Configuration - Using environment variables
spring.datasource.url=${DATABASE_URL:postgresql://ahorrartedb_user:0gJfNc8n0BFhlb5rhs44BYovmsqt7dMw@dpg-d1b0ih6uk2gs73980kkg-a/ahorrartedb}
spring.datasource.username=${DB_USERNAME:ahorrartedb_user}
spring.datasource.password=${DB_PASSWORD:0gJfNc8n0BFhlb5rhs44BYovmsqt7dMw}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings for better performance
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# Spring Data JPA Configuration
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

#Spring Data JPA Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Elements take their values from maven pom.xml build-related information
documentation.application.description=@project.description@
documentation.application.version=@project.version@

# JWT Configuration - Using environment variables
authorization.jwt.secret=${JWT_SECRET}
authorization.jwt.expiration.days=7

# Server configuration
server.port=${PORT:8080}

# Actuator configuration for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.defaults.enabled=true

# Actuator configuration for health checks
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.database.enabled=true
