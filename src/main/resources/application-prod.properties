# Production Configuration
spring.application.name=AhorrArte-Platform

# Spring DataSource Configuration - Direct configuration
spring.datasource.url=jdbc:postgresql://dpg-d1b0ih6uk2gs73980kkg-a.oregon-postgres.render.com:5432/ahorrartedb
spring.datasource.username=ahorrartedb_user
spring.datasource.password=0gJfNc8n0BFhlb5rhs44BYovmsqt7dMw
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings for better performance
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# Spring Data JPA Configuration - Explicit PostgreSQL configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.id.new_generator_mappings=true
spring.jpa.properties.hibernate.connection.characterEncoding=utf8
spring.jpa.properties.hibernate.connection.CharSet=utf8
spring.jpa.properties.hibernate.connection.useUnicode=true

# Disable conflicting auto-configurations if needed
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT Configuration - Using environment variables with fallback
authorization.jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopmentOnlyNotForProduction123456789}
authorization.jwt.expiration.days=7

# Server configuration
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/
server.tomcat.max-threads=200
server.tomcat.accept-count=100

# Actuator configuration for health checks
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
# Use the same port as the main application in Render
# management.server.port=${PORT:8080}

# Logging configuration
logging.level.com.finsuma.ahorrarte=INFO
logging.level.org.springframework.boot.autoconfigure=INFO
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.boot.web.embedded=DEBUG
