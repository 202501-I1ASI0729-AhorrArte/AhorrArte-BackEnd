# Production Configuration
spring.application.name=AhorrArte-Platform

# Spring DataSource Configuration
# The DatabaseConfiguration class will handle DATABASE_URL transformation
# These are fallback values using the correct external hostname
spring.datasource.url=${MANUAL_DB_URL:jdbc:postgresql://dpg-d1b0ih6uk2gs73980kkg-a.oregon-postgres.render.com:5432/ahorrartedb}
spring.datasource.username=${DB_USERNAME:ahorrartedb_user}
spring.datasource.password=${DB_PASSWORD:0gJfNc8n0BFhlb5rhs44BYovmsqt7dMw}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings for better performance
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# Spring Data JPA Configuration - Minimal for Spring Boot 3.x auto-configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# Disable conflicting auto-configurations if needed
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT Configuration - Using environment variables with fallback
authorization.jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopmentOnlyNotForProduction123456789}
authorization.jwt.expiration.days=7

# Server configuration
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/
server.tomcat.max-threads=200
server.tomcat.accept-count=100

# Actuator configuration for health checks
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.server.port=${PORT:8080}

# Logging configuration
logging.level.com.finsuma.ahorrarte=INFO
logging.level.org.springframework.boot.autoconfigure=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.boot.web.embedded=DEBUG
